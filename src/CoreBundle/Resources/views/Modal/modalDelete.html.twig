
<div class="modal fade" id="modalDelete" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-red no-border">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">
                    <i class="fa fa-spinner fa-spin"></i> Chargement en cours...
                </h4>
            </div>
            <div class="progress">
                <div class="progress active">
                    <div class="progress-bar progress-bar-danger progress-bar-striped"
                         role="progressbar"
                         aria-valuenow="100"
                         aria-valuemin="0"
                         aria-valuemax="100"
                         style="width: 100%"
                    ></div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default pull-right" data-dismiss="modal">Annuler</button>
            </div>
        </div>
    </div>
</div>

<script>
    //Récupère le contenu initial de la modalDelete :
    var $modalDeleteInitialContent = $('#modalDelete').find('.modal-content').html();
    //console.log($modalDeleteInitialContent);

    //Lors de la fermeture de la modalDelete, on restaure son contenu initial...
    $(document.body).on('hidden.bs.modal', function () {
        $('#modalDelete').removeData('bs.modal').find('.modal-content').html($modalDeleteInitialContent);
    });
</script>