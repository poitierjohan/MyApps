{% extends "@ERPDocument/admin.html.twig" %}

{% block metaTitle %}
    Documents
{% endblock %}

{% block title %}
    Clients
{% endblock %}

{% block titleSmall %}
    Vos clients
{% endblock %}

{% block titleAction %}
    <a href="{{ path('admin_erp_user_user_add') }}"
       class="btn btn-primary"
       data-toggle="modal"
       data-dismiss="modal"
       data-target="#modal">
        <i class="fa fa-plus-circle"></i> Nouveau
    </a>
{% endblock %}

{% block body %}

    <div class="col-md-12 no-padding">
        <div class="box box-primary">
            <div class="box-body">
                <table class="table table-striped table-hover">
                    <thead>
                    <tr>
                        <th>Code</th>
                        <th>Nom</th>
                        <th>N° TVA</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for user in users %}
                        <tr>
                            <td>
                                <a href="{{ path('admin_erp_user_user_update', {id: user.id}) }}" data-toggle="modal" data-dismiss="modal" data-target="#modal">{{ user.code }}</a>
                            </td>
                            <td>{{ user.completeName }}</td>
                            <td>{{ user.tvaNumber }}</td>
                            <td>
                                <div class="btn-group btn-group-xs">
                                    <a href="{{ path('admin_erp_user_user_update', {id: user.id}) }}" class="btn btn-default" data-toggle="modal" data-dismiss="modal" data-target="#modal"><i class="fa fa-pencil"></i></a>
                                    <a href="{{ path('admin_erp_user_user_delete', {id: user.id}) }}" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}